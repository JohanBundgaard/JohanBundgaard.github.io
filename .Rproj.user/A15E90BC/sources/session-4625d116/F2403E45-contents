# 1) Install the helper package
install.packages("installr")

# 2) Run the Git installer (follow the on-screen wizard; choose
#    "Use Git from the command line" when asked about PATH)
installr::install.git()

system("quarto preview")

# Run in terminal
cd "C:\Users\JBUN0013\Desktop\jb_site"

git init
git add .
git commit -m "Initial Quarto site"
git branch -M main
git config user.name "Johan Bundgaard"